import { WithSubscriptionHelper } from './with-subscription.helper';

describe('WithSubscriptionHelper', () => {
  it('should create an instance', () => {
    expect(new WithSubscriptionHelper()).toBeTruthy();
  });
});
